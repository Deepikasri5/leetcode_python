# Sorting solution
# 
def contains_duplicate(nums):
    nums.sort()
    for i in range(1, len(nums)):
        if nums[i] == nums[i-1]:
            return True
    return False

def contains_duplicate_hashmap(nums):
    dic = {}
    for i in range(len(nums)):
        if nums[i] in dic:
            return True
        dic[nums[i]]=i
    return False

def contains_duplicate_hashset(nums):
    seen = set()
    for num in nums:
        if num in seen:
            return True
        seen.add(num)
    return False



def main():
    nums= [1,1,1,3,3,4,3,2,4,2]
    bool = contains_duplicate(nums)
    print("Result", bool)

main()